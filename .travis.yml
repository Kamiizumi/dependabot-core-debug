services:
  - docker

script:
  - echo $TRAVIS_BUILD_DIR
  - ls
  - ls $TRAVIS_BUILD_DIR
  - docker run --rm -v $TRAVIS_BUILD_DIR:/src alpine:latest ls / && ls /src
  # - docker build -t dependabot-core-debug:latest .
  # - git clone https://github.com/dependabot/dependabot-core.git && cd dependabot-core && git checkout tags/v0.66.8
  # - docker run --rm -v $TRAVIS_BUILD_DIR:/src dependabot-core-debug:latest ls && ls /src && chmod +x /src/TestContainer.sh && /src/TestContainer.sh
